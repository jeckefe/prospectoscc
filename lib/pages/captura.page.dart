import 'package:flutter/material.dart';

class CapturaPage extends StatelessWidget {
  const CapturaPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('CapturaPage'),
      ),
    );
  }
}
