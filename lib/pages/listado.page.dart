import 'package:flutter/material.dart';

class ListadoPage extends StatelessWidget {
  const ListadoPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('ListadoPage'),
      ),
    );
  }
}
