import 'package:flutter/material.dart';

class SeguimientoPage extends StatelessWidget {
  const SeguimientoPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('SeguimientoPage'),
      ),
    );
  }
}
